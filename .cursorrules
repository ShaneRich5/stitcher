You are working on a frontend-only image carousel tool called "Stitcher".



Core principles:

- This is a client-side only application (no backend, no APIs, no authentication).

- All image processing must happen in the browser using Canvas APIs.

- Do not introduce Next.js, server frameworks, or backend services.



Architecture:

- Use Vite + React + TypeScript.

- Use TanStack Router with file-based routes under `src/routes/` (plugin generates `src/routeTree.gen.ts`).

- Use Konva.js for canvas rendering, transforms, and user interactions (carousel/composer tools).

- Use native Canvas API for image slicing/export and GIF/video frame rasterization.

- Use JSZip for exporting multiple images as a zip file.

- Use gifenc for encoding animated GIFs in the browser.

- Use Mediabunny (WebCodecs) for MP4/WebM video exports from frame sequences.



Canvas rules:

- The canvas is always the source of truth for image layout.

- Avoid manipulating image data outside the canvas pipeline.

- All exports must be derived from canvas rendering, not original image state.



UI/UX rules:

- Keep UI minimal and tool-focused (not app/platform-like).

- Prioritize drag, scale, and slice interaction simplicity.

- Use Tailwind CSS for styling when introduced; existing tools may use shared CSS in `index.css`.

- Shared chrome (nav) lives in the root route; each tool is its own route page.



Code style:

- Prefer simple functional components.

- Keep state local unless shared across canvas/editor.

- Avoid unnecessary libraries or abstractions.



Naming:

- All source filenames under `src/` use kebab-case (e.g. `multi-tile-composer.tsx`, `tile-export-zip.ts`, `app-nav.tsx`), not PascalCase or camelCase.

- Single-word names stay lowercase (`main.tsx`, `types.ts`, `index.css`).

- Exported React component identifiers remain PascalCase in code (e.g. `MultiTileComposer`).

- Exported functions/types keep their existing camelCase/PascalCase identifiers; only filenames change.

- Import paths must match the kebab-case filename (e.g. `./components/multi-tile-composer`, `./lib/export-tiles`).



Do not:

- Do not use Next.js.

- Do not add a database or persistence layer.

- Do not introduce TanStack Query or server-state libraries.

- Do not add authentication.



Goal:

Build a fast, lightweight, multi-tool image studio (carousel stitching, GIF from frames, and more) for social media.

